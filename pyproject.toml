[project]
name = "acyresys"
version = "0.1.0"
description = "Minimal ACyReSys prototype for autonomous cyber resilience simulation"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["acyresys"]
